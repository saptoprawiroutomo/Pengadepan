[phases.setup]
nixPkgs = ['nodejs-20_x', 'npm-10_x']

[phases.install]
cmds = ['cd inter-media-app && npm ci']

[phases.build]
cmds = ['cd inter-media-app && npm run build']

[start]
cmd = 'cd inter-media-app && npm start'
